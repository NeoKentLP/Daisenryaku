# 大战略 — AI 开发规则

## 工作流程

- 开始任何新步骤前必须先和用户确认
- 删除任何文件前必须先和用户确认
- 修改设计文档（DESIGN_0.1.md）前必须先和用户确认
- 觉得用户说的有歧义时，主动询问以确认明确含义
- 不主动创建 README/说明文档等非用户要求的文件
- 除非用户明确要求，不执行 git commit/push
- 完成一步再进入下一步，不并行推进多个步骤
- 功能开发完后先自行测试验证，不等用户发现 Bug

## 版本与提交

- 一个提交只做一件事，不混搭多个无关修改
- 提交信息简述改了什么 + 为什么
- 版本/分支创建前先和用户确认分支策略
- 不提交未通过测试的代码
- 大重构前先建立分支，不在主分支上直接改

## Bug 与错误处理

- 用户报告问题后，先用 editor.debug_output / 测试定位根因，再给结论
- 每个修复的 Bug 必须记录根因和修复方案于 docs/ISSUES.md §11 中
- 新增功能必须附带对应测试用例（tests/test_full.gd），覆盖正常和边界情况
- 修复 Bug 时必须附带防回归测试用例
- 难以重现的 Bug 先在测试中模拟再修
- 关键逻辑（战斗结算、AI 决策）加入调试日志
- 用 push_error() 标记不可恢复的错误，print() 标记调试信息
- 不吞错误：catch 块里至少打印日志

## UI 规范

### 布局
- 所有界面必须用 .tscn 场景文件（Control 节点树），不能用代码创建 UI 布局
- 脚本只负责逻辑（信号连接、数据填充），不负责布局
- 使用 %UniqueName 在脚本中引用场景节点：@onready var xxx = %Xxx
- 基础设计分辨率 1920×1080，用 Control 锚点自适应
- 弹窗打开时添加全屏透明 ColorRect 作为 blocker，设置 mouse_filter = STOP
- 动态创建的节点统一用 add_child() + 规范命名

### 样式
- 所有面板圆角统一：弹窗10px / 面板6px / 卡片4px
- 同类型信息用同一种样式：状态标签、属性值、技能标签等
- 可交互元素 hover 时亮度+10%，不可交互元素统一半透明 50%
- 文字层级不超过 5 级：标题(14-16px) → 区块(12px) → 正文(11px) → 辅助(10px) → 标注(8-9px)
- 同色系不并排放置（如两个绿色标签紧邻），保持视觉区分度
- 色彩从 ui_style.gd 常量取，不硬编码
- 功能色不超过 6 种：友(#44aa88) / 敌(#cc4444) / 高亮(#77aaff) / 警告(#ffaa44) / CQB(#44aa44) / 禁用
- 士气/HP 状态色符合直觉：绿=正常 / 黄=中 / 红=差 / 灰=死亡 / 橙=狂热
- 状态变化用淡入淡出而非瞬切
- 字体大小用点数（px）而非缩放，确保不同分辨率下清晰
- 中文用系统默认字体，不嵌入额外字体文件
- 英文/数字与中文混排时行高一致
- 军事术语统一用 DESIGN_0.1.md 附录C的中英文对照
- 面板背景不高于 97% 不透明，保留隐约的地图透出

### 动画与反馈
- 移动动画每步 0.1-0.3s
- 攻击/命中效果用闪光或抖动，不靠大范围粒子
- 状态变化用淡入淡出而非瞬切
- 动画用 Tween 而非手动计时器

## 代码规范

- 不用 class_name（Steam 版 Godot 不支持），用 load()/preload()
- 不用 var x := func() 推导类型
- 不用类型注解：func resolve(x, y) 而非 func resolve(x: Type, y: Type)
- 不用静态方法 + preload 调用（Steam 版不支持）
- 不用 const X = func_call()，用 static var + _init()
- 不用 "s" * N，用 "s".repeat(N)
- 鸭子类型（duck typing），不依赖类型检查
- 文件编码 UTF-8 无 BOM
- 所有数学运算注意 int/float 混用，整除用 / 100.0 而非 / 100
- 跨场景数据传递走 GameManager，不走全局变量或场景树遍历
- 战斗/技能等逻辑返回 Dictionary 而非自定义对象
- 函数不超过 50 行，超过考虑拆分
- 不用布尔参数控制行为分歧，拆成独立函数
- 不硬编码魔法数，用命名常量
- 数据文件路径用常量定义，不写死字符串
- 不用绝对路径 get_node 访问 autoload，直接用名称
- 场景切换前清理信号连接，避免访问已销毁节点
- await 后必须检查节点是否仍有效：is_instance_valid(node)
- _ready() 中谨慎使用 await，避免场景树未就绪的竞态条件

## 架构原则

- 保持模块解耦：UI 不直接操作战斗逻辑，通过 GameManager 或信号通信
- 全局状态放在 GameManager，局部状态放在各自模块
- 不假设某个库/工具可用，先检查代码库是否已在使用
- 频繁使用的场景用 preload，按需加载的用 load
- 不要每帧创建/销毁节点，用对象池或 show/hide
- 新增 GameManager 字段时同时提供 getter/setter 方法

## 数据配置规范

- 核心数值（base_damage, penetration 等）从 JSON 配置读取，不在代码中硬编码
- 部队/武器/技能数据只在一个地方定义，避免多处维护
- 配置 JSON 和代码中 weapon ID 必须一一对应
- JSON 配置修改后跑一次数据验证脚本
- 武器/单位 ID 变更时同步更新所有引用处

## 项目文件规范

- `.uid` 和 `.import` 文件由 Godot 自动管理，不手动编辑或删除
- 不手动修改 `project.godot` 中的 autoload 注册（用编辑器操作）
- `.godot/` 目录是引擎本地缓存，不提交到版本控制

## 防御性编程

- 访问对象属性前先做 null 检查
- 遍历数组时检查数组是否为空
- scene transition 前保存必要数据到 GameManager
- 鼠标点击先判断是否在有效区域再处理
- 输入必须做空值/边界检查

## 策划规范

- 新增机制前先检查是否与现有系统冲突
- 概率性事件必须在游戏内明确告知玩家或提供可查规则
- 一个版本只聚焦一个核心体验目标，不贪多

## 数值设计规范

- 数值设计必须有对照表，不拍脑袋填数
- 每次战斗公式调整后同步更新 DESIGN_0.1.md 中的对应数值
- 战斗预期结果写清楚：什么情况算正常，什么情况算 Bug
- 所有数值修改先用测试验证预期结果，再合入主分支

## 测试规范

- 测试用例分三级：
  - 烟雾（smoke）：核心流程，每次修改后必跑
  - 全量（full）：所有功能，提交前必跑
  - 压力（stress）：边界负载，大版本前跑
- Bug 分三级：
  - 崩溃（P0）：游戏阻断，立即修
  - 功能（P1）：偏离设计，当版本修
  - 视觉（P2）：显示不对，排期修
- 战斗测试必须覆盖：命中率边界（5%/95%）、穿透边界（diff ≥ +3 / ≤ -3）、士气边界（0/100）
- 随机数相关测试必须用固定种子，保证可重现
- UI 测试检查：文字不溢出容器、弹窗不超出屏幕、交互反馈正确
- 场景切换测试：每个过渡（菜单→战斗→结算）至少完整走通一次
- 每次修改后跑烟雾测试：test_smoke.tscn
- 不测 Godot 引擎行为，只测自己的逻辑
- 测试验证通过后才能声明功能完成

## 美术资源

### 文件管理
- 图片放 assets/ 下对应子目录
- 命名：全英文小写 + 下划线，如 tank_pz4.png
- 格式：PNG 透明背景
- 图标提供 1x/2x 两套，@2x 用于高 DPI 屏幕

### Hex 地图
- 所有地形瓦片用 pointy-top 六边形，外接圆直径统一
- 地形颜色与功能对应：绿=森林 / 黄=平地 / 灰=城市 / 棕=山地
- 部队图标始终居中于六边形内切圆
- 高亮/选择效果用半透明叠加层，不替换底层地形色

### 部队图标
- 不同兵种用形状区分：圆形=步兵 / 方形=载具 / 菱形=火炮 / 三角=空中
- 己方用冷色系（蓝绿），敌方用暖色系（红橙）
- 已行动部队加调暗或去色叠层，不依赖 E 标识
- 部队图标附兵力提示（数字/小点），不点开面板也知道大概状态
